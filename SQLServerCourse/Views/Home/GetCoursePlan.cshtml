@model List<string>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12 text-center text-white" style="color:white; font-size:18px;">
            @for (int i = 0; i < Model.Count() - 1; i++)
            {
                <h3 style="font-size:26px; font-weight: 600;" class="text-white">@Model[i]</h3>
                <br />
            }
        </div>
    </div>
</div>
